# papatune-flutter

## Flutter

このプロジェクトでは fvm を使用してFlutter SDKを管理しています。
flutter コマンドは必ず `fvm flutter` 経由で実行してください。

```bash
fvm flutter pub get
fvm flutter run
fvm flutter build apk
fvm flutter test
```
